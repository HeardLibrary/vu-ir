import { LocationStrategy } from '@angular/common';
import { InjectionToken } from '@angular/core';
import { NavigationEnd } from '@angular/router';
import { Observable } from 'rxjs';
import { InternalRouterConfiguration } from './configuration';
export declare const MATOMO_PAGE_URL_PROVIDER: InjectionToken<PageUrlProvider>;
export interface PageUrlProvider {
    getCurrentPageUrl(event: NavigationEnd): Observable<string>;
}
export declare class DefaultPageUrlProvider implements PageUrlProvider {
    private readonly config;
    private readonly baseHref;
    private readonly locationStrategy;
    constructor(config: InternalRouterConfiguration, baseHref: string | null, locationStrategy: LocationStrategy);
    getCurrentPageUrl(event: NavigationEnd): Observable<string>;
    private getBaseHrefWithoutTrailingSlash;
}
